#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <sys/wait.h>
#include <sys/types.h>

typedef char *Input;
/* typedef size_t Buffsize; */
typedef char *Args[];
typedef char *Env[];
typedef int Status;

extern Input input;
extern Env env;
extern ssize_t my_read;
/* extern Buffsize buffsize; */
extern Args args;
extern Status status;

#endif
